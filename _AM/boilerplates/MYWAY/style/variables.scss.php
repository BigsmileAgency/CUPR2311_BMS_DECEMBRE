$banner-w: <?= $width ?>px;
$banner-h:  <?= $height ?>px;


<?php if($format == '120x600'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 100px;
    $content-h: 390px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 90px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '160x600'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 140px;
    $content-h: 390px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 96px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '300x600'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 260px;
    $content-h: 390px;
    $padding-extra: 25px;
    <!-- // LOGO VALUE -->
    $logo-w: 130px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '300x250'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 260px;
    $content-h: 190px;
    $padding-extra: 20px;
    <!-- // LOGO VALUE -->
    $logo-w: 230px;
    $logo-h: round($logo-w*0.07826086956);

<?php } ?>
<?php if($format == '600x500'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 520px;
    $content-h: 380px;
    $padding-extra: 40px;
    <!-- // LOGO VALUE -->
    $logo-w: 460px;
    $logo-h: round($logo-w*0.07826086956);
<?php } ?>
<?php if($format == '300x100'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 235px;
    $content-h: 90px;
    $padding-extra: 5px;
    <!-- // LOGO VALUE -->
    $logo-w: 38px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '300x150'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 225px;
    $content-h: 130px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 38px;
    $logo-h: round($logo-w*1.05263158);
<?php } ?>
<?php if($format == '320x100'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 255px;
    $content-h: 80px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 38px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '320x50'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 265px;
    $content-h: 40px;
    $padding-extra: 5px;
    <!-- // LOGO VALUE -->
    $logo-w: 28px;
    $logo-h: round($logo-w*0.78571428571);

<?php } ?>
<?php if($format == '640x200'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 490px;
    $content-h: 160px;
    $padding-extra: 20px;
    <!-- // LOGO VALUE -->
    $logo-w: 85px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '728x90'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 570px;
    $content-h: 70px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 64px;
    $logo-h: round($logo-w*0.78571428571);

<?php } ?>
<?php if($format == '970x90'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 770px;
    $content-h: 70px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 64px;
    $logo-h: round($logo-w*0.78571428571);

<?php } ?>
<?php if($format == '1120x90'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 820px;
    $content-h: 70px;
    $padding-extra: 10px;
    <!-- // LOGO VALUE -->
    $logo-w: 64px;
    $logo-h: round($logo-w*0.78571428571);

<?php } ?>
<?php if($format == '840x150'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 650px;
    $content-h: 114px;
    $padding-extra: 18px;
    <!-- // LOGO VALUE -->
    $logo-w: 100px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '840x250'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 610px;
    $content-h: 170px;
    $padding-extra: 40px;
    <!-- // LOGO VALUE -->
    $logo-w: 100px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
<?php if($format == '970x250'){ ?>
    <!-- // WHITE CONTAINER VALUE -->
    $content-w: 730px;
    $content-h: 170px;
    $padding-extra: 40px;
    <!-- // LOGO VALUE -->
    $logo-w: 100px;
    $logo-h: round($logo-w*1.05263158);

<?php } ?>
